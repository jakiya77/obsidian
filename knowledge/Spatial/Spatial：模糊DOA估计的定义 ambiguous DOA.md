
**Rank one ambiguities**

公式推导：

针对一个阵列，如果出现了模糊 == 代表着阵列无法对两个方向进行区分 == 阵列对于两个方向的manifold vector出现了线性相关
$$
\vec a(\theta_1) = [1~~e^{-j 2 \pi f_cdsin\theta_1}~~e^{-j 2 \pi f_c 2dsin\theta_1}...e^{-j 2 \pi f_c ndsin\theta_1}]
$$

$$
\vec a(\theta_2) = [1~~e^{-j 2 \pi f_cdsin\theta_2}~~e^{-j 2 \pi f_c 2dsin\theta_2}...e^{-j 2 \pi f_c ndsin\theta_2}]
$$
$$
k = 2\pi f_c ~~;~~r_m =dsin\theta 
$$
则出现模糊的条件是：
$$
\alpha \vec a(\theta_1) + \beta \vec a(\theta_2) = \vec0 ~~~\theta_1 \neq \theta_2
$$
换言之，存在 
$$
(\vec k_1-\vec k_2)\vec r_m = 2P_m \pi ~~~~P_m \in integral
$$
![[截屏2025-12-08 14.56.37.png]]

综合图片内容，图中的横线代表的是K1-K2的矢量方向，如果

#### 没太看懂 太抽象了 这里的阵元到底是分布在一条线上还是分别分布在不同的平行线上？？？


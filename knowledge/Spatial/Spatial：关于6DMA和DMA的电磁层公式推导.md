在6DMA中，有$\hat{g}_{k,m}^l(A_m) \triangleq \hat{g}(\mathbf{u}_m; \psi_{k,m}^l) = \max \{ \mathbf{d}(\psi_{k,m}^l)^T \mathbf{u}_m, 0 \}$![[图片 1.png|298]]
定义的$\hat g_{k,m}$ 是当下阵元的旋转角度和信号来向之间的夹角。推广到DMA中，也就是阵元自带的阵元因子的可调角度与信号来向之间的夹角。定义为$EF(\theta,\phi)=cos(\theta-\phi)$ 

==这里定义的g是由于天线物理旋转带来的，我的DMA是由于自己设置单个阵元的角度带来的，他直接设置成cos是因为两个向量的夹角是cos 为什么我的可以设置成cos^2 或者cos的多次方呢 是什么物理原理在里面 为什么可以直接把天线旋转的角度和实际信号来向作为cos n次方的输入量 因为cos的n次方已经不具备向量夹角这个定义了 所以我认为和之前的是不等同的==

==有依据吗 参考文献 为什么6dma通过旋转之后也可以得到这种cos形式的天线增益 这算是一种广义的天线阵元的定向性 (Directivity) 或 半功率波束宽度 (HPBW)。符合余弦次幂方向图模型==



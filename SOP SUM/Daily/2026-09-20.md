

### 任务一：冻结现有仿真结果

- **时间预期**：20 分钟
    
- 今天不要再增加 Monte Carlo 次数，也不要继续修改 KL 约束。把已经得到的结果保存下来：
    

> [!info] 实验核心结果
> 
> - **Perfect-CSI平均速率增益**：1.0502 bit/s/Hz
>     
> - **State-Aware平均速率增益**：0.8744 bit/s/Hz
>     
> - **隐蔽约束**：500 组信道中未观察到 KL 约束违反
>     

- **今天的产出**：保留两份 MATLAB 代码、两份 CSV、对应图表，以及一段不超过 200 字的初步实验结论。
    

### 任务二：检查真实数据

- **时间预期**：60–90 分钟
    
- 这是今天最重要的一件事。先不下载 Scenario 24–29 的全部图像和 LiDAR 数据。我们只需要选择一个场景，下载或查看它的 CSV 和官方数据说明。
    
- 建议先检查：DeepSense Scenario 24–29
    
- **目标**：确认真实数据能够支撑什么预测任务，而不是立即训练神经网络。
    

**重点检查四项：**

- [ ] 是否有连续的 `seq_index` 和时间戳？
    
- [ ] RGB 和 LiDAR 是否与 RF 测量同步？
    
- [ ] 是否提供真实的 LoS / Blockage 标签？
    
- [ ] 每条独立序列中是否包含遮挡前、遮挡发生和遮挡恢复的过程？
    

最后一个问题尤其重要。如果一条序列只有单一的 Blocked 状态，我们就无法从中研究预测式 MA 在遮挡发生前的重构决策。

- **今天的产出**：一份数据字段表，以及一条包含遮挡状态变化的真实时间序列。
    

### 任务三：固定网络与通信优化器之间的接口

- **时间预期**：30–40 分钟
    
- 先不设计新 Transformer，也不加入完整 CSI 预测。第一版保留最简单的输入输出：
    

Plaintext

```
历史 RGB + LiDAR + RF 状态
          ↓
     多模态预测器
          ↓
```

$$\hat{\mathrm{s}}_{t+\Delta} \in \{\mathrm{LoS}, \mathrm{Blocked}\}$$

Plaintext

```
          ↓
    状态相关信道模型
          ↓
  TX-MA 位置与功率优化
```

当前 MATLAB 优化器先保留，不需要重写。但必须明确：遮挡标签只能提供传播状态，不能直接给出 Willie 的完整 MA 空间信道。我们暂时采用已校准的状态相关信道模型，后续再用真实测量校准或分析模型误差。

- **今天的产出**：一张完整系统流程图，以及明确的网络输入、输出与通信优化器接口。
    

## 3. 今天明确不做的事情

今天先不继续推导 DEP/KL，不修改 Transformer 网络结构，不增加 TX-MA 数量，也不开始复杂的多时隙轨迹优化。

这些都是后续可能需要研究的内容，但它们不是当前阻碍项目推进的主要问题。

今天真正的阻碍是：我们还没有确认 DeepSense 的真实遮挡序列怎样进入现有 MA 物理模型。

## 4. 今天结束时，你应该拿到什么？

> [!todo] 今日任务检查 (0/5)
> 
> - [ ] 现有 MA 物理仿真结果已经保存
>     
> - [ ] 确认一个可用的 DeepSense 遮挡数据场景
>     
> - [ ] 找到至少一条真实遮挡状态变化序列
>     
> - [ ] 明确网络输入和未来遮挡标签
>     
> - [ ] 明确预测输出如何进入 MATLAB MA 优化器
>     

这五件事完成之后，你的项目就不再只是两个独立的模块，而是已经具备一条可以运行的：

$$\boxed{ \text{真实感知数据} \rightarrow \text{未来传播状态} \rightarrow \text{TX-MA 隐蔽通信决策} }$$

### 今日任务清单与通关考核标准

针对今日的目标，整理为以下具体的执行任务与考核点。准备就绪后，我们将直接从 G1 启动。

  

#### **必须完成：G1 数据预处理 (`preprocess_data.py`)**

- **物理目标**：将原始连续的通信测量流，切分为“历史观测”预测“未来状态”的滑动时间窗样本。
    
      
    
- **按需补足的工程语法**：
    
      
    - 基础数据结构：`list`, 字典。
        
          
        
    - 流程控制：`for` 循环, 变量赋值。
        
          
        
    - 数据操作库：`pandas.DataFrame`, `.iloc` 行列索引, 切片操作 (`slice`), CSV读写。
        
          
        
- **通关 Checkpoint**：
    
      
    - 能够不看注释，准确描述 Raw data $\rightarrow$ Data cleaning $\rightarrow$ Sliding window $\rightarrow$ Train/Test CSV 的流转过程。
        
          
        
    - 能够手动推演切片规模：
        
          
        

$$T_{in}=8, T_{out}=5$$

计算在总帧数已知时，最终生成的样本条数。

* 能够口述指定关键行 Python 代码的作用。

  

#### **应该完成：G2 数据集映射与加载 (`data_loader.py`)**

- **物理目标**：将表格中按行记录的索引，转换为能够被 GPU 并行处理的批量（Batch）张量（Tensor）数据。
    
      
    
- **按需补足的工程语法**：
    
      
    - 面向对象编程基础：`class`, `__init__`, `__len__`, `__getitem__`。
        
          
        
    - PyTorch核心机制：`Dataset` 类的继承与重写, `DataLoader` 的工作原理。
        
          
        
    - 张量操作：List 转换为 Tensor, `torch.stack` 与张量拼接, 基础的 `transform`。
        
          
        
- **通关 Checkpoint**：
    
      
    - 清楚描述 CSV 文件中的一行，如何转变为最终输入给网络的一个 Batch。
        
          
        
    - 准确回答 `Dataset` 与 `DataLoader` 在流程中的分工。
        
          
        
    - 解释 `torch.stack(imgs)` 之前变量的形态，操作后的形态，以及为什么该场景不能直接使用 `torch.tensor()`。
        
          
        

#### **学有余力：G3 模型特征编码器 (`model.py` 前端部分)**

- **物理目标**：使用卷积网络和全连接网络，分别提取环境视觉特征和数值状态特征，并将其拼接对齐。
    
      
    
- **按需补足的工程语法**：
    
      
    - PyTorch网络搭建：`nn.Module`, 类的继承, `forward()` 前向传播的定义。
        
          
        
    - 网络组件映射：`nn.Linear` (对应感知机), 预训练 ResNet。
        
          
        
    - 张量形变操作：`.view()` 或 `.reshape()`。
        
          
        
- **通关 Checkpoint**：
    
      
    - 在不运行程序的情况下，推演以下数据流历经各代码行时的张量维度（Shape）变化：
        
          
        
    - 推演流程示例：`[B, 8, 3, H, W]` $\rightarrow$ 经过 `.view()` $\rightarrow$ 经过 ResNet $\rightarrow$ 经过 `.view()` $\rightarrow$ 最终特征维度。
        
          
        
    - 解释为什么在输入图像编码器前，需要将 Batch 维度和 Time 维度临时合并。